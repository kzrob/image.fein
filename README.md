# image.fein

A custom image file format (.fein) with an image viewer written in GTK4.

Inspired by [Facedev's bruh image format](https://github.com/face-hh/bruh)

# Libraries

`sudo dnf install gtk4-devel` -- for Fedora Linux